kind = "web"
previewPath = "/"
title = "Free Time Survey"
version = "1.0.0"
id = "artifacts/free-time-survey"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20698

[services.development]
run = "pnpm --filter @workspace/free-time-survey run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/free-time-survey", "run", "build" ]
publicDir = "artifacts/free-time-survey/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20698"
BASE_PATH = "/"
